namespace comprehensure.DASHBOARD.StoryPage
{
    public partial class QuizPage4 : ContentPage
    {
        public QuizPage4(DASHBOARD.StoryPage.QuizPage4ViewModel viewModel)
        {
            InitializeComponent();
            BindingContext = viewModel;
        }
    }
}